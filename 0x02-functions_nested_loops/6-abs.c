#include "main.h"

/**
 * _abs - compute absolute value of an int
 * @n: int type number
 *
 * Return: Absolute value of the input number.
 */
int _abs(int n)
{
	if (n < 0)
		i = i * -1;
	return (i);
}
