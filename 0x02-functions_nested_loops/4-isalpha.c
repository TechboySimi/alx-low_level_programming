#include "main h"

/**
 * isalpha - checks for alphabetic character
 *
 * Return: 1 if letter, else 0
*/

int _isalpha(int c);
{
	if ((c >= 97 && c <= 122) || (c >= 65 && c <= 132))
		return (1);
	else
		return (0);
	return (0);
}
